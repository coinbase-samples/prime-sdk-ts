export type EstimatedNetworkFees = {
  /**
   * Estimated lower bound for networks fees (in whole units)
   */
  lowerBound?: string;
  /**
   * Estimated upper bound for network fees (in whole units)
   */
  upperBound?: string;
};
